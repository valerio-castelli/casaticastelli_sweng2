@startuml

title sd_reservation_allocation

ReservationManagement -> ReservationManagement: checkReservations() //retrieve them from DB?
alt no reservation to be allocated
	
else some reservation must be allocated
	loop for all reservations to be allocated
		|||
		ref over ReservationManagement: sd_allocate
		ReservationManagement -> ReservationManagement: deleteReservation() // DB?		
	end
end
@enduml