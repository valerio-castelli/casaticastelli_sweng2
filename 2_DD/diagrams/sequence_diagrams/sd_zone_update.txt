@startuml
browser -> webadm: askUpdateForm()
webadm -> remote:
remote -> zone:
zone -> dataaccess
dataaccess -> DBMS: //delete/alternative???
dataaccess <- DBMS
browser <- webadm

loop until confirmation or exit
	browser -> browser: insertNewData
	browser -> webadm: commit(data)
	webadm -> remote:
	remote -> zone:
 
	zone -> zone: checkZoneConsistence(zone)
	alt ok
		zone -> dataaccess: write
		dataaccess -> DBMS: write()
		zone -> browser: ok
	else wrong
		zone -> browser: wrong
	end
end
	
@enduml