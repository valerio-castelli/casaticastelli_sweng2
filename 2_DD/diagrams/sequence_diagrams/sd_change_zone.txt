@startuml
TaxiDriverApp ->> comm: sendGPS(taxiId, GPS)
comm ->> TaxiManager: sendGPS(GPS, taxiId)
TaxiManager -> ZoneManager: getZone(GPS) //or directly to Mapping Service? // if outsidecity???
ZoneManager -> MappingService:  //necessary?

alt changed zone
	TaxiManager -> TaxiManager: updateZone() //DB?
	
else unchanged zone
	|||

end
@enduml