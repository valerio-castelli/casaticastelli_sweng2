Assumptions:
- all'utente che prenota con largo anticipo non importa se il taxi non arriva nei 10 minuti >> non garantiamo che arrivi nei 10 minuti
- identifare e code sono la stessa cosa
(- stuttura del codice: alfanumerico, lunghezza, ...)
- the user cannot specify which taxi he/she wants to reserve: he can only specify origin and destination of the ride


Goals:
NB: there are many queues of available taxi: one for each zone
- if a user request a ride, is request is stored in a queue of requests (waiting list???)
	-- specify / give a name to this list
- if there is a pending call in the waiting list, as soon as there is a taxi code in the queue, the taxi related to this code is associated with the first
	-- too much saying
- if a user request a ride, a taxi must arrive as soon as possible
	-- as soon as possible ??? w.r.t what "possible" ???
- if a user request a ride, the first taxi available in the queue is selected
- if a taxi driver refuse a call, it is remove from the queue and put in the last position of the queue.
- when a taxi driver accept a call, it is remove from the queue and putted in a list of unavailable taxi
	-- specify / give a name to this list
- ??? what happen to the managed calls (i.e.: when a taxi has been associated to a call) ???
- when a taxi communicate its availability, its code is stored in the queue (in the last position).
- a queue (i.e., a FIFO list) is used to store the availabilities of taxi drivers: the first taxi to communicate its availability, will be the first in the code.
	-- it is necessary to specify that when a taxi is taken from the queue, all the other are shifted??? i think no, it's the definition of queue/FIFO...
- if a user reserve a taxi, the reservation must be stored (i.e.: it will not be lost)
- if a user withdraw from a call, ???
- if a user delete a reservation,  ???
- 

- if a user has booked a ride, starting from 10 minutes before the time of the ride a taxi must b
- allow a guest to register
- allow users to:
	-- log in
	-- call for a taxi
	-- prenote a taxi
	-- withdraw from a call (???)
	-- delete a prenotation (probable yes)
- inform user when a tax is coming
	-- send them regular updates (??)
- allow taxi drives to:
	-- accept a call
	-- refuse a call
	-- communicate "user on board"
	-- communicate "user arrived at destination"
	-- communicate his availability
	-- withdraw from the queue (??)
- fair use of the queue:
	-- only taxy from the zone are selected
	-- FIFO
	-- ...
- programmatic	interfaces (is this a goal ??)

- corse extraurbane? permetterle? se sì, cosa succede squanto il taxi è fuori?
- programmatic	interfaces?
- mapping service: world or shared?
- la prenotazione è una interfaccia programmabile / modulo esterno
- come viene calcolato il waiting time?
- per chi fa richieste


Dubbi/Osservazioni
- se manca interntet? l'id come fa ed essere messo in coda?
- se nessun taxi è available (tutti impeganti in una corsa, tutti fuopri città, ...)?
- come essere sicuri che un taxista, quando ha accettatto una corsa, ci è andato veramente?
- essere sicuri che il taxista completa effettivamente la corsa
- come il taxista comunica di essere avialbale
- frequenze ricezione dati GPS (una volta al minuto?, ...)
- se il taxista spegne il cellulare / il taxista è irraggiungibile / se c'era già una corsa in corso?
	-- si scarica la batteria durante una corsa extraurbana?
- come si prenota attualmente un taxi?
- timeout >> rifiuto
- in che posizione viene messo il taxista quando accetta la richiesta?
- quando una corsa è conclusa, in che pos. è il taxi?
- il taxista può, dopo aver accettato una chiamata, rifiutarla?
- waiting time and reply message for the user: what if there is no taxi available?
	-- e se ci sono ritardi?
- se non ci sono taxi in coda?
	- do all'utente la possibilità di mettersi in coda nelle richieste, dicendolgi anche quanti utenti ci sono già in coda
		

Comportamenti utente
- l'utente può rifiutare?
	-- se riufiuta, viene inviata una notificia al taxista? il taxista deve riconfermare di essere disponibile? viene lasciato 
- se l'utente non si presenta?
- se l'utente prenota con giorni di anticipo, può ritirare la prenotazione?
- l'utente deve registrarsi (?)
- con quanti giorni di anticipo??

____________________________________________________________________________________________________

SW req.
- google mapping

HW requirement
- mobile da usare?

Scope:
- time in hh:mm (??)
- urban zone for the selection of available taxi (??)

