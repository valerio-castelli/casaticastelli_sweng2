Assumptions:
- all'utente che prenota con largo anticipo non importa se il taxi non arriva nei 10 minuti >> non garantiamo che arrivi nei 10 minuti
- identifare e code sono la stessa cosa
(- stuttura del codice: alfanumerico, lunghezza, ...)
- the user cannot specify which taxi he/she wants to reserve: he can only specify origin and destination of the ride


Goals:
- allow a guest to register
- allow users to:
	-- log in
	-- withdraw from a call (???)
	-- delete a prenotation (probable yes)
- fair use of the queue:
	-- only taxy from the zone are selected
	-- FIFO
	-- ...
- programmatic	interfaces (is this a goal ??)
- mapping service: world or shared?
- la prenotazione è una interfaccia programmabile / modulo esterno


Dubbi/Osservazioni
- se nessun taxi è available (tutti impeganti in una corsa, tutti fuopri città, ...)?
- come essere sicuri che un taxista, quando ha accettatto una corsa, ci è andato veramente?
- essere sicuri che il taxista completa effettivamente la corsa
- come il taxista comunica di essere avialbale
- frequenze ricezione dati GPS (una volta al minuto?, ...)
- se il taxista spegne il cellulare / il taxista è irraggiungibile / se c'era già una corsa in corso?
	-- si scarica la batteria durante una corsa extraurbana?
- come si prenota attualmente un taxi?
- in che posizione viene messo il taxista quando accetta la richiesta?
- quando una corsa è conclusa, in che pos. è il taxi?
- il taxista può, dopo aver accettato una chiamata, rifiutarla?
- waiting time and reply message for the user: what if there is no taxi available?
	-- e se ci sono ritardi?
- se non ci sono taxi in coda?
	- do all'utente la possibilità di mettersi in coda nelle richieste, dicendolgi anche quanti utenti ci sono già in coda
		

Comportamenti utente

- se l'utente non si presenta?
- se l'utente prenota con giorni di anticipo, può ritirare la prenotazione?
- l'utente deve registrarsi (?)

____________________________________________________________________________________________________

SW req.
- google mapping

HW requirement
- mobile da usare?

Scope:
- time in hh:mm (??)
- urban zone for the selection of available taxi (??)

